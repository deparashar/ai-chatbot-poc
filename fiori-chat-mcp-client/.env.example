# MCP Server
MCP_SERVER_URL=https://a62a414ctrial-dev-sap-mcp-server.cfapps.ap21.hana.ondemand.com

# LLM — Groq (free tier)
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL=llama-3.3-70b-versatile
LLM_API_KEY=your-groq-api-key-here

# Local dev only — a valid XSUAA Bearer token to pass to MCP server
DEV_TOKEN=

# Port (local dev only — CF uses PORT env var automatically)
PORT=8080
